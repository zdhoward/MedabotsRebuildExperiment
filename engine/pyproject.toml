[project]
name = "medarot-engine"
version = "0.1.0"
description = "Clean-room headless Medarot battle system engine"
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=8"]

[tool.pytest.ini_options]
testpaths = ["tests"]
